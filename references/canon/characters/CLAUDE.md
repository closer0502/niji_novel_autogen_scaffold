# 原作キャラクターファイル規約

## ルール

- **今作に登場する原作キャラのみ**ファイルを作成する（登場しないキャラは作らない）
- 新規キャラは `_template.md` のフォーマットをコピーして作成する
- ファイル名は `{{キャラ名}}.md`（例: `yuuki.md`, `saki.md`）
- このディレクトリの内容は**原作の公式設定のみ**を記載する（読み取り専用扱い）
- 今作での独自解釈・改変は `characters/` 側のファイルに記載する
- キャラが新たに登場する章の執筆前に、必要に応じてファイルを追加する
